package application.domain.models

data class WishlistItem(
    val id: String, // Could be product ID or a unique identifier
    val name: String,
    // Add other relevant fields like imageUrl, price if available later
)
