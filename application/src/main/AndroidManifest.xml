<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- package="com.example.application" is ignored for libraries, namespace in build.gradle is used -->

    <application>
        <!-- android:label and android:theme should be defined in the main app module -->
        <!-- If this module has its own activities, they would be declared here -->
        <!-- For example:
        <activity android:name=".SomeActivityInLibrary" />
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->
    </application>
</manifest>
